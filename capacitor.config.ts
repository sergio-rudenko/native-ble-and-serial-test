import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'network-access-controller-2wigand-client',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
